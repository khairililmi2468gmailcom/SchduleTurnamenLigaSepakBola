JPanel panel = new JPanel();	//panel pertandingan generate
		//tabbedPane.addTab("New tab", null, panel, null);
		int jml_pertandingan = 20;
		panel.setLayout(new GridLayout(1, jml_pertandingan));
		ArrayList<JPanel> list = new ArrayList<JPanel>(jml_pertandingan);
		for(int i=0; i<jml_pertandingan; i++) {
			JPanel panel_pert = new JPanel();
			list.add(panel_pert);
			list.get(i).add(panel_pert);
		}
		scrollPane.setViewportView(panel);